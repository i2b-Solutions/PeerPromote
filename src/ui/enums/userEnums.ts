/**
 * Enumeration defining different types of users.
 * Each value represents a specific user type.
 * @enum {string}
 */export enum UserTypes {
    BUSINESS = 'Business',
    CONTENT_CREATOR = 'Content Creator'
}