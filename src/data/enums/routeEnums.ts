/**
 * Enumeration defining the main routes of the application.
 * Each value represents a specific route.
 * @enum {string}
 */
export enum MainRoutes {
    HOME = '/',
    PRICING = '/pricing',
    ABOUT_US = '/about-us',
}